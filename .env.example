# Backend — copy to backend/.env and fill in
PORT=3001
NODE_ENV=development

# CORS (optional; dev allows localhost by default)
# FRONTEND_ORIGIN=http://localhost:5173

# Future: API keys (never commit real values)
# GOOGLE_AI_API_KEY=
# OPENAI_API_KEY=
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
