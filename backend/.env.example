PORT=3001
# Local: http://localhost:5173 | Production: https://dreamscometrue.work (must match the browser origin)
FRONTEND_ORIGIN=http://localhost:5173
# Local: http://localhost:8000 | Railway private: http://<ml-service-name>.railway.internal:$PORT (see Railway → ML service → Networking)
ML_SERVICE_URL=http://localhost:8000
# Same value as ML service ML_SERVICE_TOKEN
ML_SERVICE_TOKEN=dev-token

TOGETHER_API_KEY=